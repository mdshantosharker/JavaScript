function tenTimes(number) {
  const Ten = number * 10;
  return Ten;
}

function cutHalf(number) {
  const half = number / 2;
  return half;
}

const output = tenTimes(5);
console.log("output", output);
console.log(cutHalf(100));
